# Media upload backend used by the app. Must be reachable by BOTH sender and receiver.
# For local dev on a single machine:
# VITE_UPLOAD_BASE_URL=http://localhost:8787
# For LAN testing across devices, replace with your machine IP:
# VITE_UPLOAD_BASE_URL=http://192.168.1.100:8787

# Upload mode
# - simple: dev JSON API at `${VITE_UPLOAD_BASE_URL}/upload`
# - nip96:  NIP-96 multipart/form-data directly to VITE_UPLOAD_BASE_URL
# VITE_UPLOAD_MODE=simple

# Auth mode
# - none:  no Authorization header
# - token: Authorization: Bearer <VITE_UPLOAD_AUTH_TOKEN>
# - nip98: Authorization: Nostr <base64(event27235)>
# VITE_UPLOAD_AUTH_MODE=none

# Optional bearer token for the upload server (used only when AUTH_MODE=token)
# VITE_UPLOAD_AUTH_TOKEN=yourStrongToken

# Example for a NIP-96/NIP-98 server (nginx routes /upload to nip96):
# VITE_UPLOAD_BASE_URL=https://relay1.matrus.org/upload
# VITE_UPLOAD_MODE=nip96
# VITE_UPLOAD_AUTH_MODE=nip98
# VITE_UPLOAD_AUTH_TOKEN=
